[build]
  base = "apps/web"
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "../api"

# Redirect all requests to /index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
